version: '3'
services:
  app.npm:
    container_name: app.npm
    image: node
    user: "node"
    working_dir: /home/node/app
    environment:
      - NODE_ENV=development
    entrypoint: ['./nodejs.sh']
    volumes:
      - '.:/home/node/app'
      - './nodejs.sh:/home/node/app/nodejs.sh'
    ports:
      - 3002:3000
      - 3003:3001
    expose:
      - 3002
      - 3003
      - 8090
    networks:
      app:
        aliases:
          - app.npm
networks:
  app:
    driver: bridge
